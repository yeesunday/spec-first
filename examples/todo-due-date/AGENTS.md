# Todo example rules

1. 先读 `spec/CONVENTIONS.md`，再按自己的角色查看 `spec-map.yml` 中的 Spec。
2. 产品行为以 `REQ-due-date` 和 `ACCEPTANCE.md` 为准；API、数据、设计以各自目录里的原生文件为准。
3. 发现行为缺口、文档冲突或公共契约变化时，先更新 Spec 并走 PR。
4. 完成后运行 `npm test`，报告 Spec ID、修改文件和验证结果。
