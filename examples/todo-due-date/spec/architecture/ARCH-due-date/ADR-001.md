# ADR-001：展示层派生日期状态

- 状态：accepted
- 关联：`ARCH-due-date`、`REQ-due-date#R2`

## 背景

“今天到期”和“已过期”取决于用户所在时区，同一个日期不应在数据库里保存多份派生状态。

## 决策

数据库只保存日期；展示层以用户时区计算徽标。排序仍由数据库按原始日期执行。

## 后果

- 好处：避免跨时区状态过期和批量重算；
- 代价：每个展示端必须使用统一日期工具，并覆盖时区测试。
