# Todo 项目 Spec 约定

遵循仓库级 [写作与目录约定](../../../docs/spec-writing.md)。

- 目录名必须等于 frontmatter 的 `spec.id`；
- 需求条款使用 `R1`、`R2`，验收使用 `AC-1`，测试使用 `TC-1`；
- 原生 OpenAPI、SQL、tokens 和设计资源保留在对应 Spec 目录；
- `approved` 表示约定可被消费，不代表代码已经上线；
- 实现状态以 PR、测试和 Change 任务为准。
